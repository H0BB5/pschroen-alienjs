# Examples

## Component catalog

[`catalog`](catalog) is a standalone Next.js application generated from the local Aliencn registry. It exercises every dashboard and experiential component without modifying the generated source.

From the repository root:

```sh
npm install
npm run example:dev
```

The catalog runs at [http://localhost:3000](http://localhost:3000).
